# Replace the landing video

## Changes
- Upload the provided MP4 as the landing page’s new Band video.
- Replace the current scroll-controlled video source with the uploaded clip.
- Remove the fallback poster image so there is no backup hero visual.
- Keep the existing landing layout and scroll behavior unchanged.

## Verification
- Check that the new video renders on the landing page without a poster fallback.
- Verify desktop and mobile framing, scrolling, and the latest preview status.
